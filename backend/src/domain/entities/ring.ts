import { Forger } from "./forger"

export interface Ring {
    ringId: string
    name: string
    power: string
    proprietor: string
    image: string
    forgerId: string
    createdAt: Date
    updatedAt: Date
}