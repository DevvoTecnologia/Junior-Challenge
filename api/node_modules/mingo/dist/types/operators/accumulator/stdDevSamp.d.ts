import { AccumulatorOperator } from "../../core";
/**
 * Returns the sample standard deviation of the input values.
 * @param  {Array} collection
 * @param  {Object} expr
 * @return {Number|null}
 */
export declare const $stdDevSamp: AccumulatorOperator<number>;
