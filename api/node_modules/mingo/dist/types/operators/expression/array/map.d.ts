import { ExpressionOperator } from "../../../core";
/**
 * Applies a sub-expression to each element of an array and returns the array of resulting values in order.
 *
 * @param obj
 * @param expr
 * @returns {Array|*}
 */
export declare const $map: ExpressionOperator;
