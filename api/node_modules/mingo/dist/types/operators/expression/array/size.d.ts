import { ExpressionOperator } from "../../../core";
/**
 * Counts and returns the total the number of items in an array.
 *
 * @param obj
 * @param expr
 */
export declare const $size: ExpressionOperator;
