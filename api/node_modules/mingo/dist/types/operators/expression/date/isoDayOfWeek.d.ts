import { ExpressionOperator } from "../../../core";
/**
 * Returns the weekday number in ISO 8601 format, ranging from 1 (Monday) to 7 (Sunday).
 * @param obj
 * @param expr
 */
export declare const $isoDayOfWeek: ExpressionOperator<number>;
