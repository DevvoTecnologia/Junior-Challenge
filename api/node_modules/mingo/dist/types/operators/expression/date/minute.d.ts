import { ExpressionOperator } from "../../../core";
/**
 * Returns the minute for a date as a number between 0 and 59.
 * @param obj
 * @param expr
 */
export declare const $minute: ExpressionOperator<number>;
