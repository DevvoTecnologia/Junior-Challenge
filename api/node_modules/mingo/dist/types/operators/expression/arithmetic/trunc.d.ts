import { ExpressionOperator } from "../../../core";
/**
 * Truncates a number to a whole integer or to a specified decimal place.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare const $trunc: ExpressionOperator;
