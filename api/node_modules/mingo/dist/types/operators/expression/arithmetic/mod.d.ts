import { ExpressionOperator } from "../../../core";
/**
 * Takes two numbers and calculates the modulo of the first number divided by the second.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare const $mod: ExpressionOperator;
