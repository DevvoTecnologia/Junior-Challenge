import { ExpressionOperator } from "../../../core";
/**
 * Calculates the square root of a positive number and returns the result as a double.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare const $sqrt: ExpressionOperator;
